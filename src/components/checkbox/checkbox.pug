
mixin checkbox(options = {})
  -
    const {
      label = undefined,
      sublabel = undefined,
      checked = false
    } = options

  .checkbox   
    label.checkbox__label !{sublabel ? `<span class='checkbox__main-label'>${label}</span><br><span class='checkbox__sublabel'>${sublabel}</span>` : label}
      input.checkbox__field(
        type = "checkbox"
        checked = checked
      )