
mixin radio(options = {})
  -
    let {
      label = undefined,
      name = undefined,
      checked = false
    } = options

  .radio   
    label.radio__label !{label}
      input.radio__field(
        type = "radio"
        name = name
        checked = checked
      )