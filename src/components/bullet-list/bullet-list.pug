mixin bullet-list(options)
  if !options
    - options = {}
    
  .bullet-list
    .bullet-list__title.h3= options.title
    ul.bullet-list__list
      - for (let item of options.items)
        - let text = item
        li.bullet-list__item !{text}